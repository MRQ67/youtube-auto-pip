{
  "name": "youtube-auto-pip",
  "version": "1.0.0",
  "description": "Automatically enables Picture-in-Picture mode for YouTube videos when switching tabs",
  "main": "background.js",
  "scripts": {
    "build": "zip -r youtube-auto-pip.zip . -x '*.git*' '.*' 'node_modules/*' 'package-lock.json' 'yarn.lock'",
    "test": "echo 'No tests specified'"
  },
  "keywords": [
    "chrome-extension",
    "youtube",
    "picture-in-picture",
    "pip",
    "video"
  ],
  "author": "YouTu Developer",
  "license": "MIT",
  "dependencies": {
    "canvas": "^3.2.0"
  }
}
